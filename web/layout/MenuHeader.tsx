import styled from "@mui/material/styles/styled";
import ListSubheader from "@mui/material/ListSubheader";

export const MenuHeader = styled(ListSubheader)({
  height: 40,
});

export default MenuHeader;
