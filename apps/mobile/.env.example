# Saturn Mobile Environment Configuration

# By default, the app automatically detects your computer's LAN IP from the Expo Metro bundler connection.
# (e.g., http://<YOUR_COMPUTER_IP>:8080)
#
# If you want to explicitly override the backend API URL, uncomment and set this:
# EXPO_PUBLIC_API_URL=http://<YOUR_COMPUTER_IP>:8080
